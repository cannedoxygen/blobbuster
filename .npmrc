# Force rebuild of native dependencies on install
# Ensures bcrypt, sharp, and other native modules are built for the deployment platform
build-from-source=true

# Avoid optional dependency failures
optional=false

# Ensure all workspace dependencies are installed
prefer-workspace-packages=true
