# Use prebuilt binaries for native dependencies when available
# This avoids needing Python/build tools at install time
prefer-offline=false

# Ensure all workspace dependencies are installed
prefer-workspace-packages=true
