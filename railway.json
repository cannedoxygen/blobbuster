{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=@blobbuster/shared-types && npm run build --workspace=@blobbuster/backend"
  },
  "deploy": {
    "startCommand": "exec node apps/backend/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
