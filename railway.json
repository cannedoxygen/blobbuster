{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=@blobbuster/shared-types && npm run build --workspace=@blobbuster/backend && cd apps/backend && npx prisma generate"
  },
  "deploy": {
    "startCommand": "node apps/backend/start.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
